class Main {

    function void main() {
        var Console console;

        do Const.init();
        do lib.init();
        do Str.init();
        do Vector.init();
        do Error.init(Const.errTab(), 2);
        do Eval.init();
        do Canvas.init(2, 32, 20, 31, ">", "|");
        do Console.init();

        do Console.run();

        do Canvas.dispose();
        do Parser.dispose();
        do Error.dispose();

        return;
    }

}
