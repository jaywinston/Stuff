class Main {

    function void main() {

        do Const.init();
        do lib.init();
        do Str.init();
        do Vector.init();
        do Error.init(Const.errTab(), 2);
        do Eval.init();
        do Canvas.init(2, 32, 20, 31, ">", "|");
        do Terminal.init(2, 1, 20, 30);
        do Console.init();

        //do Canvas.dispose();
        //do Parser.dispose();
        //do Error.dispose();

        return;
    }

}
